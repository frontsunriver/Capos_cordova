import { ProductVariantProductModel } from './product-variant-product-model';

describe('ProductVariantProductModel', () => {
  it('should create an instance', () => {
    expect(new ProductVariantProductModel()).toBeTruthy();
  });
});
