import { ProductVariantModel } from './product-variant-model';

describe('ProductVariantModel', () => {
  it('should create an instance', () => {
    expect(new ProductVariantModel()).toBeTruthy();
  });
});
