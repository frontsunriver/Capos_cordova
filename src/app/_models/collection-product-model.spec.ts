import { CollectionProductModel } from './collection-product-model';

describe('CollectionProductModel', () => {
  it('should create an instance', () => {
    expect(new CollectionProductModel()).toBeTruthy();
  });
});
