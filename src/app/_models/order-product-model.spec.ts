import { OrderProductModel } from './order-product-model';

describe('OrderProductModel', () => {
  it('should create an instance', () => {
    expect(new OrderProductModel()).toBeTruthy();
  });
});
