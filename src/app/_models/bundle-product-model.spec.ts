import { BundleProductModel } from './bundle-product-model';

describe('BundleProductModel', () => {
  it('should create an instance', () => {
    expect(new BundleProductModel()).toBeTruthy();
  });
});
