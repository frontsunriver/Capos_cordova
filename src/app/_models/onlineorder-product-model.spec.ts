import { OnlineorderProductModel } from './onlineorder-product-model';

describe('OnlineorderProductModel', () => {
  it('should create an instance', () => {
    expect(new OnlineorderProductModel()).toBeTruthy();
  });
});
